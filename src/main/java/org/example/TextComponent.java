package org.example;

/**
 * Base interface for all text components (letters, words, symbols, sentences).
 * Represents a hierarchical structure of text elements.
 */
public interface TextComponent {
    /**
     * Converts the component to its string representation.
     *
     * @return string representation of the component
     */
    String toString();
}
